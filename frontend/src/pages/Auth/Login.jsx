import React from "react";
import LoginForm from "./LoginForm";

const Login = () => {
  // LoginForm will be rendered by the Outlet in AuthPageLayout
  return <LoginForm />;
};

export default Login;
